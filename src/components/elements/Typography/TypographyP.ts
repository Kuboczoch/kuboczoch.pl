import styled from '@emotion/styled'

const TypographyP = styled('p')`
  display: inline-block;
  font-size: 1em;
  font-weight: 400;
  margin: 0 0 15px;
  color: ${(props) => props.theme.colors.gallery};
  font-family: Pixel, monospace;
`

export default TypographyP
